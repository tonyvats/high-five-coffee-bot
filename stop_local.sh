#!/bin/bash
# Остановка локальных процессов: админка + бот

echo "🛑 Останавливаем High Five Coffee..."

pkill -f "hfctbot.py" 2>/dev/null && echo "  ✓ Бот остановлен" || echo "  — Бот не был запущен"
pkill -f "admin.app" 2>/dev/null && echo "  ✓ Админка остановлена" || echo "  — Админка не была запущена"
lsof -ti :5050 | xargs kill -9 2>/dev/null

echo "✅ Готово"

# ✅ Чеклист перед деплоем

## На сервере

- [ ] Docker установлен
- [ ] Порты 5050 и 8000 открыты в firewall
- [ ] Репозиторий склонирован (`git pull` для обновления)

## Переменные

```bash
export BOT_TOKEN="прод_токен"      # от @BotFather (НЕ dev!)
export ADMIN_IDS="462076,306535565"
export ADMIN_PASSWORD="сложный_пароль"
```

## Команды

```bash
./deploy.sh
```

## После деплоя

- [ ] Бот отвечает в Telegram
- [ ] Админка открывается: http://IP:5050
- [ ] Вход по паролю работает

## Если уже был старый деплой

Старый бот остановится. База `menu.db` создастся заново с дефолтным меню (seed). Если нужно сохранить изменения из админки — сделайте бэкап `menu.db` до обновления.
